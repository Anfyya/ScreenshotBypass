# 变更记录

## v0.1 - 2026-03-30 15:29
- 变更摘要：初始化 Git 仓库并准备首次推送到远端仓库 `Anfyya/ScreenshotBypass`。
- 涉及文件：`CHANGELOG.md`
- 原因：按项目要求记录本次变更，并将当前工程纳入版本控制以便推送到 GitHub。
- 如何部署更改：首次执行 `git init -b main`、`git add -A`、`git commit -m "Initial commit"`、`git remote add origin https://github.com/Anfyya/ScreenshotBypass.git`、`git push -u origin main`；后续同步执行 `git add -A && git commit -m "<说明>" && git push origin main`。
